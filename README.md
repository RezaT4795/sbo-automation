# SBo Automation

Personal automation for maintaining my SlackBuilds.org packages.

Currently focused on Microsoft .NET SlackBuilds, including:

- dotnet-runtime
- aspnetcore-runtime
- dotnet-sdk

The scripts automate version checks, SlackBuild metadata updates, checksum refreshes, Git branch creation, pull request creation, and optional local package installation.
